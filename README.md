# Wolf-of-AI-Street
A LSTM Model + Bot that predict the value of EUR/USD in the next hour on a 1 to -1 normalized confidence scale (1 meaning very confident for Upward, 0 neutral/unstable, -1 very confident for downward) (Model) and make investments based on that data (bot) 

To run model. In project root bash, run: python main.py